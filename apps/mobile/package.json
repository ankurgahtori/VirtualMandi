{
  "name": "@virtual-mandi/mobile",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "tsc --noEmit --watch",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  }
}
